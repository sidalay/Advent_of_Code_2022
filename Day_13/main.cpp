#include <iostream>
#include <fstream>
#include <string>
#include <string_view>
#include <vector>

int main() 
{
    std::ifstream Input{"input.txt"};
    std::string Buffer{};

    while (std::getline(Input, Buffer)) {
        
    }
}